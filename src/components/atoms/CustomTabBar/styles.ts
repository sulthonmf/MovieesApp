import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  absolute: {
    height: 100
  },
  tabContainer: {
    backgroundColor: 'transparent',
    bottom: 0,
    height: '100%',
    overflow: 'hidden',
    position: 'absolute',
    width: '100%'
  },
});
