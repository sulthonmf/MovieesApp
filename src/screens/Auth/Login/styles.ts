import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  bottomText: {
    marginTop: 20,
  },
  container: {
    backgroundColor: '#000',
    flex: 1,
    justifyContent: 'center',
    padding: 16,
  },
  input: {
    backgroundColor: 'white',
    borderColor: '#ccc',
    borderRadius: 5,
    borderWidth: 1,
    height: 50,
    marginBottom: 16,
    padding: 12,
  },
  link: { color: 'white', fontSize: 14, marginTop: 8, textAlign: 'center' },
  title: {
    color: 'white',
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 24,
    textAlign: 'center',
  },
});
